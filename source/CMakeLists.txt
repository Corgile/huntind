AUX_SOURCE_DIRECTORY(. SOURCE)
SET(SOURCE_TARGET hd_source)

ADD_SUBDIRECTORY(type)
ADD_SUBDIRECTORY(encoder)
ADD_SUBDIRECTORY(sink)
ADD_SUBDIRECTORY(business)

ADD_LIBRARY(${SOURCE_TARGET} STATIC ${SOURCE})

TARGET_LINK_LIBRARIES(${SOURCE_TARGET} PRIVATE hd_type)
TARGET_LINK_LIBRARIES(${SOURCE_TARGET} PRIVATE hd_business)
TARGET_LINK_LIBRARIES(${SOURCE_TARGET} PRIVATE hd_encoder)
TARGET_LINK_LIBRARIES(${SOURCE_TARGET} PRIVATE hd_sink)
TARGET_LINK_LIBRARIES(${SOURCE_TARGET} PRIVATE pcap)