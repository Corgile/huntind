if(NOT WIN32)
    string(ASCII 27 Esc)
    set(reset "${Esc}[m")
    set(bold  "${Esc}[1m")
    set(Red         "${Esc}[31m")
    set(Green       "${Esc}[32m")
    set(Yellow      "${Esc}[33m")
    set(Blue        "${Esc}[34m")
    set(Magenta     "${Esc}[35m")
    set(Cyan        "${Esc}[36m")
    set(White       "${Esc}[37m")
    set(BR          "${Esc}[1;31m")
    set(BG          "${Esc}[1;32m")
    set(BY          "${Esc}[1;33m")
    set(BB          "${Esc}[1;34m")
    set(BM          "${Esc}[1;35m")
    set(BC          "${Esc}[1;36m")
    set(BW          "${Esc}[1;38m")
endif()