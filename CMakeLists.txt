CMAKE_MINIMUM_REQUIRED(VERSION 3.25)
PROJECT(huntd)

SET(BINARY_TARGET hd-main)
SET(CMAKE_CXX_STANDARD 20)

# @formatter:off
AUX_SOURCE_DIRECTORY(${PROJECT_SOURCE_DIR}      SOURCE_FILES)
# @formatter:on

ADD_EXECUTABLE(${BINARY_TARGET} main.cpp ${SOURCE_FILES})
